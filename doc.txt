comando para iniciar la aplicacion

    uvicorn main:app --reload

operations es lo mismo que metodos http

8 operations:
    get
    post
    put
    delete

    operations
    head
    patch
    trace

path parameter -> variable definida dentro de un path {}

query parameters -> parametros opcionales ? y  & 


Request  -> comunicacion cliente servidor
Response -> comunicacion servidor cliente

Request body 

    body  de una peticion http, en formato JSON

models

    descripcion de una entidad en codigo


validaciones de query parameters

    max_length
    min_length
    regex 
    ge  - greater or equal than
    le    less or equal than  
    gt   greater than
    lt   less than

    (parametros para documentar mejor los query parameters)
    title 
    description 

validaciones de path parameters

tipos de datos exoticos (pydantic)

    enum
    HtttpUrl
    FilePath
    DirectoryPath
    EmailStr
    Payment Card Number
    IPv Any address
    Negative float
    Positive float
    Negative int
    Positive int